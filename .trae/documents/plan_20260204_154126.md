## 1. 移除侧边栏自动隐藏逻辑 (Disable Auto-Hide)
- **修改文件**: `content_gemini.js`
- **操作**: 
    - 删除 `init` 函数中的 `checkSidebar` 定义、调用及相关的 `ResizeObserver` (Line 22-43)。
    - 删除 `injectFolderUI` 函数中的 `ResizeObserver` (Line 406-425)。
- **目的**: 彻底回退 v27 引入的“响应式隐藏”特性。无论侧边栏处于何种状态，只要 DOM 结构允许，强制显示文件夹 UI。这能解决“没有正常展示”的问题。

## 2. 验证与保留核心修复 (Verify Critical Fixes)
- **检查**: 确认 `injectFolderUI` 末尾的 `renderSharedGems()` 和 `renderFolders()` 调用被保留（修复 v29 的渲染时机问题）。
- **检查**: 确认 `renderSharedGems` 中的 Session Storage 逻辑和“点击重命名”引导文案被保留。

## 3. 版本发布
- 升级版本号至 **3.0.0.30**。
- 更新 CHANGELOG.md。
- 打包生成 zip。