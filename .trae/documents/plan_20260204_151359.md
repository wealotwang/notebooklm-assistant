完全理解，使用母语（中文）来定义 Skill 的名称和描述，不仅能提高您的阅读效率，还能让我在理解上下文时更精准地命中这些 Skill。

我将对所有现有的 Skill 进行**全中文本地化 (Localization)**。

### 📋 汉化与优化计划

我将遍历 `.trae/skills/` 目录下的所有 Skill 文件，并执行以下修改：

#### 1. `收尾工作` (Wrap Up)
*   **名称**：保持 `收尾工作` (已是中文)。
*   **修改点**：将内部英文描述 (`Description`, `Trigger`, `Steps`) 全部翻译为通俗易懂的中文，并保留核心的技术逻辑。

#### 2. `保存记忆` (Save Memory)
*   **名称**：保持 `保存记忆` (已是中文)。
*   **修改点**：将刚刚生成的英文描述（如 "Auto-Save Context Protocol"）汉化，特别是关于“主动触发”的规则，用中文写得更明确。

#### 3. `版本发布` (Release Master)
*   **名称**：保持 `版本发布` (已是中文)。
*   **修改点**：
    *   将 "Sanitization Rules" 改为 **“脱敏与清洗规则”**。
    *   将触发条件（Post-Plan, Builder Mode）用中文详细描述。

#### 4. `产品设计` (Product Design)
*   **名称**：保持 `产品设计` (已是中文)。
*   **修改点**：
    *   将 "Universal Triggers" 改为 **“全场景触发机制”**。
    *   将 PRD 模板中的英文标题（User Story, AC）改为中文（用户故事、验收标准）。

### 🛠 执行步骤
1.  **读取**：逐个读取上述 4 个 Skill 的定义文件。
2.  **重写**：将内容替换为纯中文版本，确保语气自然、逻辑清晰。
3.  **验证**：确认文件名和目录结构不受影响，仅修改 Markdown 内容。

---

**您同意我立即执行这个“全中文汉化”行动吗？**
确认后，您看到的将是一个纯中文、无障碍的 Skill 体系。