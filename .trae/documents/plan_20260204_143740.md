## 1. 恢复 v24 原生 UI (Visual Upgrade)
- **动作**: 将 v3.0.0.24 `styles.css` 中的 `.gemini-folder-container` 相关样式（胶囊形状、悬停效果、排版间距）完整移植到当前版本。
- **目标**: 让共享 Gem 的列表项看起来和 Gemini 原生的 "Gems" 列表完全一致（大圆角、Google 灰背景）。

## 2. 增强菜单交互 (Interaction Upgrade)
- **动作**: 修改 `content_gemini.js` 中的 `showSharedGemMenu` 函数。
- **新增**: 在菜单中添加 **"✏️ 重命名"** 选项。点击后弹出 `prompt` 输入框，允许用户修改 Gem 名称并实时保存。

## 3. 优化名字抓取 (Logic Refinement)
- **动作**: 更新 `isInvalidName` 黑名单，强制屏蔽 "Chats"、"Chat"。
- **动作**: 优化“手动固定”按钮逻辑，点击时不再盲目自动抓取，而是**直接弹出输入框**（预填当前探测到的名字），让用户确认后再保存。

## 4. 版本发布
- 升级版本号至 **3.0.0.26**。
- 更新 CHANGELOG.md。
- 打包生成 zip。